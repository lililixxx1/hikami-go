/hikami
/hikami-api
/hazel.db
/hazel.db-*
/hikami.db
/hikami.db-*
/huizeman/
/logs/
dist/
tmp/
.env
config.yaml
cmd/hikami/webdist/
# .claude/ 整个目录是 Claude Code 的工作缓存(index.json 扫描缓存 + plans/ 会话产物),不应入库
.claude/
.spec-workflow/
cover.out
cover_final.out

# Runtime artifacts
snapshot.txt
*.network-request
# 裁剪版 ffmpeg 产物入库（让 make build-windows-amd64 开箱即用），其余临时文件忽略
internal/runtime/assets/*
!internal/runtime/assets/ffmpeg.zip
!internal/runtime/assets/LICENSE.txt

# 裁剪版 ffmpeg 构建中间产物（scripts/build-ffmpeg-minimal.sh）
# .tmp/ 整个目录是 ad-hoc 调试/临时评审工作区(shot 脚本、read_cookie 实验、
# 临时 review diff 等),全部不入库。注:第 10 行的 tmp/(无前导点)是另一条独立规则。
.tmp/

# Runtime data (recordings, transcriptions, cookies)
# data/ 是文档约定的 output_root;hikami-go/ 是 config.go 的 output_root 默认值
# (与 module 同名,git 不会自动忽略,历史上曾导致录播产物误入暂存区)。
# 两者都忽略,覆盖用户用默认值或文档示例值的场景。
data/
hikami-go/

# Config export snapshots
*-config-*.json

# Ad-hoc investigation / working notes
OPUS_DRAFT_EMPTY_CONTENT_INVESTIGATION.md
WEB_OPTIMIZATION_SUGGESTIONS.md
直播回顾_*.md

# Agent / review working notes(plans/archive/ 放行:已落地的历史计划归档)
plans/*
!plans/archive/
reviews/

# MCP / tool caches
.playwright-mcp/
.serena/
.local/

# Test data
test-recap/

# Temp plan files
plan_*.md

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store

# AI skills (samber/cc-skills-golang, local install via skills CLI)
.agents/
.claude/skills/
skills-lock.json

# Build artifacts & runtime outputs
node_modules/
output/
*.exe
*.log
/draft-render-fullpage.png
/glossary-editor.png
/recap-models-dropdown.png

# Local environment & tooling state
.cc-connect/
.config/

# Backup snapshots (runtime/config/db with timestamp)
*.bak
*.bak.*
hazel.bak*
hikami.bak*
hazel.db.bak*
hikami.db.bak*
config.yaml.bak*

# Codex task workspace
.cgpt/

# Codex review reports (codex-review skill 产物,开发过程文件)。
# reviews/ 规则已在上方忽略(任意深度),此处历史上重复一条,2026-07-25 清理时删除。

# ZCode agent 工作目录(会话计划/缓存,不应入库)
.zcode/

