## 当前任务：调用 Codex 审核之前的系统托盘计划

用户要求先验证 codex-review 能否使用、用什么模型，然后调用 codex 审核之前提出的"Windows 系统托盘 + 日志文件"计划。

### 已确认的信息

**Codex 路由配置**（`~/.codex/codex-review-routes.json`）：
- 路由队列（按优先级）：① kedaya ② tcdmx ③ pppzzz
- 模型：`gpt-5.5`，reasoning_effort: `high`
- 三条路由均 enabled

**Codex 全局配置**（`~/.codex/config.toml`）：
- 默认 model: `gpt-5.6-sol`，model_provider: `kedaya`
- 三条路由 base_url 分别为 kedaya/tcdmx/pppzzz，均用 `gpt-5.5` 模型
- 三条路由都需要 OPENAI_API_KEY（从 auth.json 读取，脚本会按路由自动切换 key）

### 执行步骤

1. 先 `--dry-run` 确认脚本能正常工作（不耗额度）
2. 将之前的计划写入临时文件，用 `--exec` 模式（read-only sandbox）让 Codex 审核计划文本
3. 读取审核结果，汇报给用户

注：由于当前没有未提交的代码改动（计划还没实现），不能用 `--review` 模式审 diff，改用 `--exec` 模式让 Codex 审核计划文档本身。