用户要求先用 codex 审核本计划再决定是否执行。为调用 codex-review skill(需执行 Bash 脚本,plan mode 下不允许),请求退出 plan mode。

退出后的动作:把「子文件夹文档漂移全面修复计划」交给 codex-review 做独立第三方审核(对照仓库实际状态抽查 10 条事实基准 + 评估范围/风险),拿到 VERDICT 后再根据 codex 意见修订计划,重新提交给用户批准。

计划本身不变(见上一条 ExitPlanMode 的完整内容),本轮只是插入 codex 审核环节,不执行任何文件改动。